# Copy this file to .streamlit/secrets.toml and fill in your key.
# secrets.toml is gitignored — never commit the real file.
# On Streamlit Cloud, add this under App Settings > Secrets instead.

FRED_API_KEY = "your_fred_api_key_here"
